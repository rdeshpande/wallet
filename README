Wallet

by Rohan Deshpande

Install using ./install.sh

# IRC

Create a .irssi.private file with something like the following:

servers = (
  {
    address = "irc.freenode.org";
    chatnet = "freenode";
    password = "mypassword";
    port = "8001";
    autoconnect = "true";
  }
);

== OTHER DEPS

- install xcode
- install brew
